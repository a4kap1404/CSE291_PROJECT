# Scripts to run for data generation

# Set variables in set_variables.sh (do not have to run)
# test_mode=1 during test runs.

get_openroad_run.sh # run placement
get_get_graph_label.sh # extract graph data and labels
